/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/http/client";
import type { RequestConfig, ResponseErrorConfig } from "@/http/client";
import type { GetUsersQueryResponse } from "../types/GetUsers.ts";

function getGetUsersUrl() {
  const res = { method: 'GET', url: `/users/` as const }  
  return res
}

/**
 * @description Get all users (admin/owner only)
 * @summary Get all users
 * {@link /users/}
 */
export async function getUsers(config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const res = await request<GetUsersQueryResponse, ResponseErrorConfig<Error>, unknown>({ method : "GET", url : getGetUsersUrl().url.toString(), ... requestConfig })  
  return res.data
}